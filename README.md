## Node for Autocomplete search the posts list

Before Starting to run, install the required packages by using npm i command 

## Available Scripts

In the project directory, you can run:

### `npm start`
